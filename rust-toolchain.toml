# Pinned during T001 on 2026-09-23. This is the toolchain actually installed and
# used on the development machine; it is not a guessed minimum supported version.
[toolchain]
channel = "1.95.0"
components = ["rustfmt", "clippy"]
