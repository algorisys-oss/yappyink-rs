[package]
name = "ink-app"
description = "Controller and mode coordinator. Pure state transitions with no OS, window, GPU, or portal dependency."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
ink-core.workspace = true
# Typed contracts only: capability states and error classes. ink-platform links
# no toolkit, so NFR-004's headless requirement still holds.
ink-platform.workspace = true
